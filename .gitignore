.caches/
.venv/
outputs/
assets/mujoco_menagerie/
__pycache__/
*.pyc

# raw per-frame capture images from hardware/repeat runs - regenerate via collect.py/collect_v2.py,
# keep meta.json/captures.jsonl/results*.json (the actual measurements) tracked
hardware/repeat/runs/*/[0-9][0-9][0-9].png

# per-project Codex home (holds auth.json with the API key) - never commit
.codex/
