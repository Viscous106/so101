[project]
name = "so101-lab"
version = "0.1.0"
description = "SO-101 / LeRobot playground: MuJoCo sim-first, CPU-only"
requires-python = ">=3.12,<3.13"
dependencies = [
    "mujoco>=3.2",
    "numpy<2.3",
    "imageio[ffmpeg]",
    "matplotlib",
]

[tool.uv]
package = false
